<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Nuevo contacto - ContactFlow</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/style.css">
</head>
<body class="bg-gradient-to-b from-gray-900 via-gray-800 to-black text-white min-h-screen flex items-center justify-center p-4">
  <div class="w-full max-w-md">
    <form action="/guardar" method="POST" class="bg-gray-800/60 p-6 rounded-2xl border border-gray-700 backdrop-blur-sm">
      <h2 class="text-2xl font-bold mb-4">Agregar contacto</h2>

      <label class="block text-sm mb-1">Nombre</label>
      <input name="nombre" required class="w-full p-3 rounded bg-gray-700 mb-3 placeholder-gray-400" placeholder="Juan" />

      <label class="block text-sm mb-1">Apellido</label>
      <input name="apellido" required class="w-full p-3 rounded bg-gray-700 mb-3 placeholder-gray-400" placeholder="Pérez" />

      <label class="block text-sm mb-1">Número</label>
      <input name="numero" required pattern="[0-9+\- ]+" title="Solo números, +, - o espacios" class="w-full p-3 rounded bg-gray-700 mb-4 placeholder-gray-400" placeholder="+1 809 555 1234" />

      <div class="flex gap-3">
        <button type="submit" class="flex-1 bg-green-600 hover:bg-green-700 px-4 py-3 rounded-full">Guardar</button>
        <a href="/" class="flex-1 text-center bg-red-600 hover:bg-red-700 px-4 py-3 rounded-full">Regresar</a>
      </div>
    </form>
  </div>
</body>
</html>
